# Application Configuration
NEXT_PUBLIC_APP_NAME=Golden West Business News
NEXT_PUBLIC_APP_VERSION=1.0.0

# Optional: Database (if using AWS RDS)
# DATABASE_URL=postgresql://username:password@host:port/database

# Optional: Authentication (if using AWS Cognito)
# NEXT_PUBLIC_COGNITO_USER_POOL_ID=your_user_pool_id
# NEXT_PUBLIC_COGNITO_CLIENT_ID=your_client_id

# Optional: Storage (if using AWS S3 uploads)
# NEXT_PUBLIC_S3_BUCKET_NAME=gwbn-storage
# NEXT_PUBLIC_S3_REGION=us-west-1

# AWS Configuration (required for Bedrock and Transcribe)
AWS_REGION=us-west-1
ACCESS_KEY_ID=your_access_key_here
SECRET_ACCESS_KEY=your_secret_key_here

# AWS Bedrock Configuration
# Note: Bedrock models need to be enabled in your AWS account
# Default model: anthropic.claude-3-sonnet-20240229-v1:0
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-20250514-v1:0

# Optional: DynamoDB Tables (only needed for advanced features)
# ARTICLES_TABLE=gwbn-articles
# USERS_TABLE=gwbn-users
# ANALYTICS_TABLE=gwbn-analytics

# Optional: API Gateway (if using AWS API Gateway)
# NEXT_PUBLIC_API_GATEWAY_URL=https://your-api-gateway-url.amazonaws.com

# Optional: AWS IAM Role (if using IAM roles instead of access keys)
# ROLE_ARN=arn:aws:iam::account:role/role-name
# WEB_IDENTITY_TOKEN_FILE=/path/to/token/file

# Note: This application works without any environment variables!
# All transcription and AI features use local processing.
# Environment variables are only needed for optional AWS integrations.